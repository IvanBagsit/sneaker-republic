{
    "version": 2,
    "builds": [
        {
            "src": "server.js",
            "use": "@now/node-server"
        }
    ],
    "routes": [
        { "src": "/public/(.*)", "dest": "/public/$1" },
        { "handle": "filesystem" },
        { "src": "/.*", "dest": "server.js" }
    ]
}